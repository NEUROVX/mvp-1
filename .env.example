# Google Stitch MCP (https://stitch.withgoogle.com → Settings → API Keys)
# Read by .mcp.json via ${STITCH_API_KEY}. Export it in your shell, or set it as an
# environment secret for Claude Code on the web. Never commit the real value.
STITCH_API_KEY=
